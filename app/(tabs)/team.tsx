import { AppText, Screen } from '@/src/components';

export default function TeamTab() {
  return (
    <Screen>
      <AppText variant="title">Team</AppText>
      <AppText variant="body" tone="muted">
        Roster lands in a later PR.
      </AppText>
    </Screen>
  );
}
