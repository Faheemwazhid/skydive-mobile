import { AppText, Screen } from '@/src/components';

export default function ChatsTab() {
  return (
    <Screen>
      <AppText variant="title">Chats</AppText>
      <AppText variant="body" tone="muted">
        Threads land in a later PR.
      </AppText>
    </Screen>
  );
}
