import { AppText, Screen } from '@/src/components';

export default function TemplatesTab() {
  return (
    <Screen>
      <AppText variant="title">Templates</AppText>
      <AppText variant="body" tone="muted">
        Catalog lands in a later PR.
      </AppText>
    </Screen>
  );
}
